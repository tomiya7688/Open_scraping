{
  "name": "open-scraping-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.15 <25"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @open-scraping/contracts && npm run build --workspace @open-scraping/core && npm run build --workspace @open-scraping/host && npm run build --workspace @open-scraping/api-client && npm run build --workspace @open-scraping/cli && npm run build --workspace @open-scraping/gui",
    "typecheck": "npm run build --workspace @open-scraping/contracts && npm run build --workspace @open-scraping/core && npm run typecheck --workspaces --if-present",
    "check:boundaries": "node scripts/check-boundaries.mjs",
    "check:docs": "node scripts/check-docs.mjs",
    "test:prototype": "node prototypes/adr-0001-stdio-component/host.mjs",
    "test": "npm run check:boundaries && npm run check:docs && npm run test:prototype && npm run test --workspaces --if-present",
    "verify": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "@types/node": "24.13.6"
  }
}
